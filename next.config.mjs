/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // img取得許可
    domains: [
      "source.unsplash.com",
      "cdn.pixabay.com"
    ],
  },
};

export default nextConfig;
